import type { Lesson } from '../../../models/lesson.model';

/** Sección Recursos: la vista muestra enlaces externos (resources.data.ts), no lista de lecciones. */
export const RECURSOS_LESSONS: Lesson[] = [];
